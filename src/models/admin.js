// const connection = require('./connection');

// const adminSeed = async () => {
//     const admin = { name: 'admin', email: 'root@email.com', password: 'admin', role: 'admin' };
//     const db = await connection();
//     await db.collection('users').insertOne(admin);
// };

// module.exports = {
//     adminSeed,
// };
